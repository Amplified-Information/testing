Simulator for the CLOB

Simulates ~10 orders/sec to your CLOB using the gRPC API. This uses the @grpc/grpc-js and @grpc/proto-loader packages.

# Quickstart

Ensure node.js version is >= 24

`nvm use v24`

`npm run sim`
